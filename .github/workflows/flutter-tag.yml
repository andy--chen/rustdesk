name: Flutter Tag Build

on:
  workflow_dispatch:

jobs:
  run-custom-rustdesk-build:
    uses: ./.github/workflows/flutter-build.yml
    secrets: inherit
    with:
      upload-artifact: true
      upload-tag: ${{ github.ref_name }}
      desktop_only: true
